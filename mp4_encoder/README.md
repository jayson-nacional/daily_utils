# Usage  
1. Clone the repository  
2. Make the script executable. ```chmod +x mp4_encoder.sh```
3. (Optional) Copy to path to execute anywhere ```sudo mv mp4_encoder.sh /usr/local/bin/[your_script_alis]```
